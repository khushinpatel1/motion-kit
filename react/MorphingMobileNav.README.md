# Morphing Mobile Nav Pill (React)

## Use it

Use <MorphingMobileNav tabs={[...]} />.

Import the component and tokens.css globally in the consuming app.

## What's tunable

Pass tab labels; indicator width is measured from the active button.

## Notes

The active state is a stateful button selection.

