# Cinematic Type Reveal (React)

## Use it

Use <CinematicTypeReveal lines={[...]} />.

Import the component and tokens.css globally in the consuming app.

## What's tunable

Pass any line array; reveal delay is capped in the component.

## Notes

IntersectionObserver reveals on entry; reduced motion shows final state.

