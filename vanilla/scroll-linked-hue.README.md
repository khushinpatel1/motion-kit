# Scroll-Linked Hue / Opacity Background (vanilla)

## Use it

Mount a `.shb-background` element.

## What's tunable

Set its starting `--hue` and `--alpha` values.

## Notes

A passive scroll listener is rAF-throttled; `destroy()` removes it.

