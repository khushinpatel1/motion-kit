# Magnetic Action Button (vanilla)

## Use it

Call `mountMagneticActionButton(button)` on a real button.

## What's tunable

Pass `radius` and `strength`; keep the pull subtle.

## Notes

Pointer logic is skipped on touch/no-hover devices and reset on leave.

