# Dimensional Tilt Card (vanilla)

## Use it

Call `mountDimensionalTiltCard(card)` with a `.dt-inner` child.

## What's tunable

Pass `maxTilt`; the inner layer’s parallax is CSS-driven.

## Notes

Reduced motion skips pointer listeners and keeps the card flat.

