# Cursor Spotlight Card (vanilla)

## Use it

Import the CSS and call `mountCursorSpotlightCard(root)`.

## What's tunable

Set the card’s base gradient and spotlight color.

## Notes

The listener only attaches for hover-capable pointers and `destroy()` removes it.

