# Staggered Scroll Reveal (vanilla)

## Use it

Add `data-stagger-item` to any group’s children and mount the group.

## What's tunable

The delay cap is set in JS; layout and transform distance are CSS-owned.

## Notes

Works for arbitrary item counts and reveals each item once.

