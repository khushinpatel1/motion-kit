# Particle Cursor Trail (vanilla)

## Use it

Call `mountParticleCursorTrail()` and retain the returned `destroy()` handle.

## What's tunable

Pass `color` and `maxParticles` to bound work.

## Notes

Heaviest effect: canvas plus rAF. Reduced motion never starts the loop.

