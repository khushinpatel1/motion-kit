export function mountStaggeredScrollReveal(root) { if (!root) return { destroy() {} }; const items = [...root.querySelectorAll("[data-stagger-item]")]; const reduced = window.matchMedia("(prefers-reduced-motion: reduce)").matches; items.forEach((item, index) => item.style.setProperty("--ssr-delay", reduced ? "0" : `calc(var(--motion-instant) * ${Math.min(index, 5)})`)); const observer = new IntersectionObserver((entries) => entries.forEach((entry) => { if (entry.isIntersecting) { entry.target.classList.add("is-visible"); observer.unobserve(entry.target); } })); if (reduced) items.forEach((item) => item.classList.add("is-visible")); else items.forEach((item) => observer.observe(item)); return { destroy() { observer.disconnect(); } }; }
