# Floating Card (vanilla)

## Use it

CSS only; add class `fc-card`.

## What's tunable

`--motion-ambient` controls the idle loop; tune surface styles around it.

## Notes

The idle animation is explicitly disabled for reduced motion.

