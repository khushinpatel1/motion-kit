# Liquid Mode Toggle (vanilla)

## Use it

Use a button with `lm-toggle` and `aria-pressed`, then mount it.

## What's tunable

Provide `onChange` to connect the mode state.

## Notes

It is organic visually, but the control remains a real button.

