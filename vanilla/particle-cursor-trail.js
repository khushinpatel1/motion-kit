export function mountParticleCursorTrail({ color = "#a7f3d0", maxParticles = 80 } = {}) {
  const canvas = document.createElement("canvas"); canvas.className = "pct-canvas"; document.body.append(canvas); const reduced = window.matchMedia("(prefers-reduced-motion: reduce)").matches; if (reduced) return { destroy() { canvas.remove(); } };
  const context = canvas.getContext("2d"); let frame = 0; let running = true; const particles = []; const resize = () => { canvas.width = innerWidth * devicePixelRatio; canvas.height = innerHeight * devicePixelRatio; context.setTransform(devicePixelRatio, 0, 0, devicePixelRatio, 0, 0); }; const move = (e) => { for (let i = 0; i < 3; i += 1) particles.push({ x: e.clientX, y: e.clientY, size: 2 + Math.random() * 4, life: 1, vx: (Math.random() - .5) * 1.4, vy: (Math.random() - .5) * 1.4 }); if (particles.length > maxParticles) particles.splice(0, particles.length - maxParticles); }; const draw = () => { if (!running) return; context.clearRect(0, 0, innerWidth, innerHeight); particles.forEach((p) => { p.x += p.vx; p.y += p.vy; p.life -= .025; context.globalAlpha = Math.max(0, p.life); context.fillStyle = color; context.beginPath(); context.arc(p.x, p.y, p.size * p.life, 0, Math.PI * 2); context.fill(); }); context.globalAlpha = 1; frame = requestAnimationFrame(draw); }; resize(); addEventListener("resize", resize); addEventListener("pointermove", move); frame = requestAnimationFrame(draw); return { destroy() { running = false; cancelAnimationFrame(frame); removeEventListener("resize", resize); removeEventListener("pointermove", move); canvas.remove(); } };
}
