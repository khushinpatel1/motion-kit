export function mountScrollLinkedHue(root) { if (!root) return { destroy() {} }; if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) { root.style.setProperty("--hue", "220"); root.style.setProperty("--alpha", ".18"); return { destroy() {} }; } let frame = 0; const update = () => { frame = 0; const max = document.documentElement.scrollHeight - innerHeight; const progress = max ? scrollY / max : 0; root.style.setProperty("--hue", String(Math.round(220 + progress * 110))); root.style.setProperty("--alpha", String(.14 + progress * .2)); }; const onScroll = () => { if (!frame) frame = requestAnimationFrame(update); }; addEventListener("scroll", onScroll, { passive: true }); update(); return { destroy() { removeEventListener("scroll", onScroll); if (frame) cancelAnimationFrame(frame); } }; }
