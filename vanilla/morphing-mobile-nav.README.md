# Morphing Mobile Nav Pill (vanilla)

## Use it

Create `.mn-nav` with `.mn-indicator` and `.mn-tab` buttons, then mount it.

## What's tunable

Tab padding determines measured indicator width.

## Notes

The active state is semantic via `.is-active`; keyboard activation comes from buttons.

