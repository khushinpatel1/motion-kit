# Cinematic Type Reveal (vanilla)

## Use it

Wrap each line in `.ctr-line`, then mount the parent.

## What's tunable

The delay cap is in the mount function; adjust shared token values globally.

## Notes

IntersectionObserver reveals lines once; reduced motion shows the final state.

