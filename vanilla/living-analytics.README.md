# Living Analytics Bars + Counter (vanilla)

## Use it

Use `.la-chart`, `.la-count`, and `.la-bar` elements with `--target`, then mount.

## What's tunable

Pass the target `value`; set each bar’s `--target` from 0 to 1.

## Notes

IntersectionObserver triggers once; reduced motion jumps to the final number.

